const Footer = () => {
  return (
    <footer className="border-t border-forest/10 bg-white/60 px-4 py-6 sm:px-6">
      <div className="mx-auto max-w-6xl text-sm text-ink/65">
        Sahayata is a frontend-only demo for discovering government schemes using
        simple eligibility questions.
      </div>
    </footer>
  );
};

export default Footer;
